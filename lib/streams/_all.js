'use strict';

const BulkStream = require('./BulkStream');
const IndexStream = require('./IndexStream');
const ScrollerStream = require('./ScrollerStream');

module.exports = {
    BulkStream,
    IndexStream,
    ScrollerStream
};